# Gemma-4-12B-it (bản mới nhất), LoRA bf16. So với Gemma-3-12B.
experiment:
  id: gemma4-12b_lora-r16_lr2e4_ep2

model:
  name: google/gemma-4-12b-it     # verify repo id chính xác trên HF khi chạy
  load_in_4bit: false
  max_seq_length: 1024

method: lora

train:
  per_device_train_batch_size: 4
  gradient_accumulation_steps: 8
